package Framework;

public class HomePage extends BasePage {
	
	     @FindBy(xpath =)
         private WebElement WOMEN;
	     @FindBy(xpath =)
         private WebElement WOMEN;
	     @FindBy(xpath =)
         private WebElement WOMEN; 
	     @FindBy(xpath =)
         private WebElement WOMEN;
	     @FindBy(xpath =)
         private WebElement WOMEN;
	     @FindBy(xpath =)
         private WebElement WOMEN;
	     
public HomePage()  {
	PageFactory.initElements(driver, this);
}
public WebElement WOMEN() {
return WOMEN;
}
public WebElement DRESSES() {
	DRESSES.click();
    return DRESSES;
}
public WebElement SHIRTS() {
	
	
    return SHIRTS;   
}
      public WebElement PRODUc

}
